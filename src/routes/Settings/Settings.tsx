import React from 'react';

const Settings: React.FC = () => {
    return (
        <div>
            <h1>Settings Page</h1>
            {/* content */}
        </div>
    );
};

export default Settings;
